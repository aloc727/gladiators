# Clash Royale API Configuration
# 
# SECURITY WARNING:
# - NEVER commit this file with real values
# - NEVER share your API key
# - Copy this file to .env and add your actual key
# - .env is already in .gitignore

# Get your API key from: https://developer.clashroyale.com/
CLASH_ROYALE_API_KEY=your-api-key-here

# Optional: Set port (default: 3000)
# PORT=3000

# Optional: Restrict CORS to specific origin (default: *)
# ALLOWED_ORIGIN=http://localhost:3000

# Analytics page (password-protected). Set in .env and in Netlify env.
# ANALYTICS_PASSWORD=your-analytics-password
# ANALYTICS_SESSION_SECRET=random-secret-string-for-cookie
